# Claude Code context

Before editing, inspect the repository and read `AGENTS.md`, `ARCHITECTURE.md`, `DECISIONS.md`, and
`ROADMAP.md`. Treat those files as authoritative project context. Follow the provider provenance and
validation rules exactly, preserve existing work, run all documented validation commands, and leave
a concise handoff.
